const header = new Vue({
    el: '#header',
    data: {
        searchInput: '',

        privateState: {},
        sharedState: store.state
    },
    methods: {
        test: function() {
        }
    }
});